name: first_work 

on:
  push:
    branches:
      - main

jobs:
  exmp-job:
    runs-on: ubuntu-latest 


    steps:
      - name: Print welcom sidoooo
        run: echo "welcom sido how are you"
        
      - name: checkout repo 
        uses:  actions/checkout@v6.0.1 # call this from the serch panal in the right  sido

      - name: list repo files 
        run: ls -la
    
  
